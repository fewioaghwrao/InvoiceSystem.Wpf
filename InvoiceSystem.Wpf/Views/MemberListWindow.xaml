<Window x:Class="InvoiceSystem.Wpf.Views.MemberListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="会員一覧"
        Height="720"
        Width="1180"
        WindowStartupLocation="CenterScreen"
        Background="#020617">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E2E8F0" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#059669" />
            <Setter Property="BorderBrush" Value="#059669" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="SubButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#E2E8F0" />
            <Setter Property="Background" Value="#0F172A" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#FFE4E6" />
            <Setter Property="Background" Value="#4C0519" />
            <Setter Property="BorderBrush" Value="#BE123C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#0F172A" />
            <Setter Property="Foreground" Value="#E2E8F0" />
            <Setter Property="BorderBrush" Value="#1E293B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="#0F172A" />
            <Setter Property="AlternatingRowBackground" Value="#111827" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#1E293B" />
            <Setter Property="VerticalGridLinesBrush" Value="#1E293B" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#111827" />
            <Setter Property="Foreground" Value="#94A3B8" />
            <Setter Property="BorderBrush" Value="#1E293B" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="12,10" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,10" />
        </Style>
        <Style x:Key="WhiteComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#0F172A"
                BorderBrush="#1E293B"
                BorderThickness="1"
                CornerRadius="16"
                Padding="20">
            <StackPanel>
                <TextBlock Text="会員一覧（管理者）"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="White" />
                <TextBlock Margin="0,6,0,0"
                           Text="会員の基本情報・ロール・有効 / 無効状態を検索し、退会（無効化）操作を行います。"
                           Foreground="#94A3B8"
                           FontSize="12" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Background="#0F172A"
                BorderBrush="#1E293B"
                BorderThickness="1"
                CornerRadius="16"
                Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.3*" />
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="1.3*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="キーワード（名前 / メール）"
                               FontSize="11"
                               Foreground="#CBD5E1" />
                    <TextBox Margin="0,6,0,0"
                             Height="34"
                             Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}"
                             Background="#020617"
                             Foreground="#E2E8F0"
                             BorderBrush="#334155"
                             Padding="10,6" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="ロール"
                               FontSize="11"
                               Foreground="#CBD5E1" />
                    <ComboBox Margin="0,6,0,0"
          Style="{StaticResource WhiteComboBoxStyle}"
          ItemsSource="{Binding RoleOptions}"
          SelectedItem="{Binding SelectedRole}"
          DisplayMemberPath="Label" />
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="ステータス"
                               FontSize="11"
                               Foreground="#CBD5E1" />
                    <ComboBox Margin="0,6,0,0"
          Style="{StaticResource WhiteComboBoxStyle}"
          ItemsSource="{Binding StatusOptions}"
          SelectedItem="{Binding SelectedStatus}"
          DisplayMemberPath="Label" />
                </StackPanel>

                <Button Grid.Column="3"
                        Margin="0,22,10,0"
                        Height="34"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Content="検索"
                        Command="{Binding SearchCommand}" />

                <Button Grid.Column="4"
                        Margin="0,22,0,0"
                        Height="34"
                        Style="{StaticResource SubButtonStyle}"
                        Content="リセット"
                        Command="{Binding ResetCommand}" Cursor="Hand" />
            </Grid>
        </Border>

        <Border Grid.Row="4"
                Background="#0F172A"
                BorderBrush="#1E293B"
                BorderThickness="1"
                CornerRadius="16"
                Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#3F0F172A"
                        Padding="14,10"
                        BorderBrush="#1E293B"
                        BorderThickness="0,0,0,1">
                    <DockPanel>
                        <TextBlock Text="会員一覧"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding PageInfoText}"
                                   Foreground="#94A3B8"
                                   FontSize="11"
                                   HorizontalAlignment="Right" />
                    </DockPanel>
                </Border>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Members}"
                          SelectedItem="{Binding SelectedMember}"
                          Margin="0"
                          Background="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70" />
                        <DataGridTextColumn Header="名前" Binding="{Binding Name}" Width="180" />
                        <DataGridTextColumn Header="メールアドレス" Binding="{Binding Email}" Width="*" />
                        <DataGridTextColumn Header="ロール" Binding="{Binding RoleText}" Width="120" />
                        <DataGridTextColumn Header="状態" Binding="{Binding StatusText}" Width="100" />

                        <DataGridTemplateColumn Header="操作" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="詳細"
                                                Margin="0,0,8,0"
                                                Padding="10,5"
                                                Style="{StaticResource SubButtonStyle}"
                                                Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}" />

                                        <Button Content="退会"
                                                Padding="10,5"
                                                Command="{Binding DataContext.DisableMemberCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource DangerButtonStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CanDisable}" Value="False">
                                                            <Setter Property="IsEnabled" Value="False" />
                                                            <Setter Property="Opacity" Value="0.45" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#FCA5A5"
                           Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}" />
                <TextBlock Text="※ 管理者は退会不可 / 無効・退会済み会員は退会操作不可"
                           Margin="0,6,0,0"
                           FontSize="11"
                           Foreground="#64748B" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="← 前へ"
                        Margin="0,0,8,0"
                        Style="{StaticResource SubButtonStyle}"
                        Command="{Binding PrevPageCommand}" />
                <Button Content="次へ →"
                        Style="{StaticResource SubButtonStyle}"
                        Command="{Binding NextPageCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
